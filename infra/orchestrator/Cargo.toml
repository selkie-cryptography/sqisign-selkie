[package]
name = "orchestrator"
version = "0.0.0"
edition = "2021"
publish = false

# Long-lived HTTP service deployed to Fly. Receives `workflow_job`
# webhooks from GitHub, mints JIT runner configs, spawns ephemeral
# Fly Machines.
#
# Exposes a small library (RunnerSizes, MachineSize, …) so the
# `render-actionlint` admin binary in `ops/` can reuse the loader
# without duplicating the TOML schema.

[dependencies]
anyhow = "1"
axum = "0.7"
hmac = "0.12"
# 11 ships no signing backend by default. aws-lc-rs over rust_crypto:
# the latter's `rsa` crate carries RUSTSEC-2023-0071 (Marvin timing
# leak) and this key signs the GitHub App JWT.
jsonwebtoken = { version = "11", features = ["aws_lc_rs"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
time = { version = "0.3", default-features = false, features = ["parsing"] }
tokio = { version = "1", features = ["full"] }
toml = { version = "0.8", default-features = false, features = ["parse"] }
tower = "0.5"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
