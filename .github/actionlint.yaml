# Generated by `cargo run -p ops --bin render-actionlint`.
# Source of truth: `infra/orchestrator/runners.toml`.
# `infra-ci.yml` runs `--check` to fail loudly on drift.
self-hosted-runner:
  labels:
    - fly
    - perf-2x
    - shared-2x
    - shared-4x
